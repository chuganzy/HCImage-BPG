language: objective-c
osx_image: xcode7.1
env:
  matrix:
    - PLATFORM=iOS
    - PLATFORM=Mac
script:
- set -o pipefail
- cd Example/"$PLATFORM"
- pod install
- xctool -workspace Example.xcworkspace -scheme Example
